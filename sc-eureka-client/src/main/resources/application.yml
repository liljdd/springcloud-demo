server:
  port: 8762

spring:
  application:
    name: sc-service-hi

# 配置spring cloud 服务发现
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
